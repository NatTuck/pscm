
Here are the basic types:
    VALUE (Any)
    SYMBOL
    STRING
    INT
    FLOAT
    CONS
    NIL

TODO:
    BOOL

Standard "methods":
    make_x
    cleanup
    type_name



annotated function

/* add_int <::> prelude/+ : Int Int -> Int */
int
add_int(int x, int y)
{
    return x + y;
}

automatically generated wrapper

sp_v*
add_int__iii(sp_v* x, sp_v* y)
{
    int x_ = unwrap_ps_int(x);
    int y_ = unwrap_ps_int(y);
    int rv_ = add_int(x_, y_);
    return pscm_make_int(rv_);      
}

