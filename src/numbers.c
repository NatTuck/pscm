
int
int_add(int x, int y)
{
    return x + y;
}

int
int_sub(int x, int y)
{
    return x - y;
}

int
int_mul(int x, int y)
{
    return x * y;
}

int
int_div(int x, int y)
{
    return x / y;
}

int
int_eq(int x, int y)
{
    return x == y;
}
